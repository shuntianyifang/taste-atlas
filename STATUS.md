# Taste Atlas 当前状态

更新日期：2026-09-14。

## 定位与实现

当前是 Windows 本地音乐分析与试听工具，未来目标是结合音乐、图片和用户反馈探索个人审美。图片分析、个人偏好学习和跨媒介推荐尚未实现，不应作为已提供功能宣传。

|能力|实际状态与边界|
|---|---|
|音频读取|MP3、FLAC 等格式；元数据、选段和全曲入口，单次最多 600 秒|
|声学特征|节拍、频谱、能量、音高候选；复杂混音可能存在倍速／半速和跟踪错误|
|语义分析|CLAP、MuQ-MuLan、Essentia 三模型；乐器、音色、情绪与主题候选；分数不是准确率|
|情绪曲线|DEAM 愉悦度／激活度及时间轴；保留越界预测，不代表听众实际感受|
|结构与和声|变化边界、相似片段、大小三和弦与调性候选；不是准确曲式或乐谱|
|分离与旋律|HTDemucs 四声部、pYIN 与 MELODIA 候选及合成音；存在串音，不恢复原始分轨|
|试听与反馈|本机 HTTP、自定义播放控件、切轨、段落／A/B 播放、喜欢／不喜欢标记导出|
|报告|Markdown、JSON、CSV、PNG、分析／试听音轨；生成数据留在本地|

## 已有验证

原工作目录已完成两首真实音乐的全曲三模型及深度分析、相关自测和文件校验。试听修复后实际验证了暂停、恢复、连续切轨保留位置、片段终点停止和标记落盘。没有做跨浏览器验证、全曲连续播放验收或人工听感质量评测。浏览器内核崩溃根因未确定，采用自定义控件避开原生控件触发路径。

复制到本项目时共 115,869 个文件、15,515,551,180 字节，逐文件 SHA-256 一致；四个复制环境可启动。复制不是一次全新的干净安装验收，也没有重新运行所有模型。详细历史记录保留本地，不随源码包发布。

## 源码发布准备

- 原代码、测试脚本、依赖锁文件、模型准备脚本及第三方说明纳入显式 SOURCE_FILES.json。
- .gitignore 默认拒绝未列入清单的文件；新增源码需要更新清单和忽略规则。
- tools/source_release.py 验证清单、大小、路径和常见凭据特征，并仅打包清单文件。
- 模型、四个环境、缓存、node_modules、参考音频、音乐图片、结果和复制清单继续保留本机，排除在发布边界之外。
- 公开源码目标为 https://github.com/shuntianyifang/taste-atlas，默认分支 main；原创代码采用 AGPL-3.0-only，保留完整 MELODIA 集成。只发布源码，不创建安装包、Release 或在线服务。

## 尚未完成

2026-09-20 新增旋律跟踪诊断及原混音／other 分轨同参数对照。The Edge 喜欢区间的候选覆盖约 54%／50%，分轨路线最长漏检更长，未替换默认算法；现有 pYIN 在所测上下文没有通过阈值的候选。已完成真实本地对照及诊断回归测试，尚无人工旋律真值，不能宣称识别准确度提高。本轮新增试听的浏览器验收因工具启动失败未完成。

2026-09-19 已新增片段级听感实验：相邻窗口变化选择、前后试听、观察／解释／待确认分栏、分轨槽位变化、带原因的区间反馈及复听对照。Arcahv、Pinnacle 各 4 个片段完成本地 HTDemucs 推理，来源与试听副本指纹已核对；浏览器验证片段停止、分轨播放、反馈恢复和本机保存。分离纯度及听感解释仍未人工验证，未建立个人审美画像。生成数据保留本机，复现入口见音频 README。

听感导读已接入基础报告及三模型报告，提供候选氛围、质感和复听时间区间；属于现有结果的证据整理，尚未完成真实听感的人工验证。深度报告仍保留技术与试听用途。

1. 从源码包在干净 Windows 环境安装与端到端推理验收。
2. Linux/macOS、GPU、跨浏览器兼容性验证和持续集成。
3. 人工标注数据上的精度评估、个人审美画像与图像分析。
4. 正式发行版本管理和安装器；原创代码已选择 AGPL-3.0-only，第三方模型等仍独立遵循各自许可。

当前适合发布为实验性源码项目，不应宣称为成熟的审美预测产品或跨电脑免安装应用。

## 本次源码边界验收

- 通过临时 Git 元数据检查实际工作目录的候选文件，源码清单与 Git 可添加文件集合一致；这项检查不依赖项目自身的 Git 初始化状态。
- 通过模型、虚拟环境、音乐、图片、结果、node_modules、复制清单与 .env 的忽略检查。
- 源码 ZIP 的文件名集合和每个文件内容均与允许清单一致；已有输出拒绝覆盖。
- 可重复运行：`python tools/test_source_release.py`（需要 Git），以及 `python tools/source_release.py --check`。
- 此次未修改音乐分析算法；没有以打包检查替代干净安装和真实模型验收。
